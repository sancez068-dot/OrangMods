services:
  - type: web
    name: orangmods-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEFAULT_ADMIN_USERNAME
        value: Ad09oLq@Gmail.yandex
      - key: DEFAULT_ADMIN_PASSWORD
        value: pa9w9diqllOoeje
      - key: ALLOWED_ORIGINS
        value: "*"
      - key: ALLOWED_HOSTS
        value: "*"
      - key: MAX_LIMIT
        value: "100"
    # ДОБАВЬТЕ ЭТИ СТРОКИ:
    environment:
      - key: PYTHON_VERSION
        value: 3.11.0
